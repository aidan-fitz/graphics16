import matrix

class Stack:
    def __init__(self):
        self.stack = []
        I = new_matrix()
        ident()
        self.stack.append(I)

    def push():
        clone = [row[:] for row in self.stack[-1][:]]
        self.stack.append(clone)

    def pop():
        if len(stack) > 1:
            return self.stack.pop()
